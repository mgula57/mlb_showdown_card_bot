{
  "name": "mlb-showdown-card-bot",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install --production=false && npm run build",
    "heroku-postbuild": "npm run build"
  },
  "engines": {
    "node": "22.x",
    "npm": "10.x"
  },
  "devDependencies": {
    "typescript": "~5.8.3"
  }
}